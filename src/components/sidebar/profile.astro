---
import { Icon } from "astro-icon/components";

import { profileConfig } from "@/config";
import { getAnalyticsConfig } from "@utils/analytics";
import { url } from "@utils/url";
import { i18n } from "@i18n/translation";
import I18nKey from "@i18n/i18nKey";
import ImageWrapper from "@/components/common/imageWrapper.astro";


const { enabled: analyticsEnabled, platform, baseUrl, apiKey, websiteId } = getAnalyticsConfig();

interface Props {
    class?: string;
    style?: string;
    side?: string;
}
const { class: className, style, side = "default" } = Astro.props;
const id = `profile-${side}`;
---

<div id={id} data-swup-persist={id} class:list={["profile-card card-base p-3", className]}>
    <a aria-label="Go to About Page" href={url('/about/')}
       class="group block relative mx-auto mt-1 lg:mx-0 lg:mt-0 mb-3
       max-w-48 lg:max-w-none overflow-hidden rounded-xl active:scale-95 profile-avatar-link">
        <div class="absolute transition pointer-events-none group-hover:bg-black/30 group-active:bg-black/50
        w-full h-full z-50 flex items-center justify-center">
            <Icon name="fa6-regular:address-card"
                  class="transition opacity-0 scale-90 group-hover:scale-100 group-hover:opacity-100 text-white text-5xl">
            </Icon>
        </div>
        <ImageWrapper src={profileConfig.avatar || ""} alt="Profile Image of the Author" class="mx-auto lg:w-full h-full lg:mt-0" loading="eager"></ImageWrapper>
    </a>
    <div class="px-2">
        <div class="font-bold text-xl text-center mb-1 dark:text-neutral-50 transition">{profileConfig.name}</div>
        <div class="h-1 w-5 bg-(--primary) mx-auto rounded-full mb-2 transition"></div>
        <div class="text-center text-neutral-400 mb-2.5 transition">{profileConfig.bio}</div>
        <div class="flex gap-2 justify-center mb-1">
            {profileConfig.links.length > 1 && profileConfig.links.map(item =>
                    <a rel="me" aria-label={item.name} href={item.url} target="_blank" class="btn-regular rounded-lg h-10 w-10 active:scale-90 profile-social-link">
                        <Icon name={item.icon} class="text-[1.5rem]"></Icon>
                    </a>
            )}
            {profileConfig.links.length == 1 && <a rel="me" aria-label={profileConfig.links[0].name} href={profileConfig.links[0].url} target="_blank"
                                            class="btn-regular rounded-lg h-10 gap-2 px-3 font-bold active:scale-95 profile-social-link">
                <Icon name={profileConfig.links[0].icon} class="text-[1.5rem]"></Icon>
                {profileConfig.links[0].name}
            </a>}
        </div>
        {analyticsEnabled && (
            <hr class="my-2 border-t border-dashed border-gray-300 dark:border-gray-700" />
            <div class="text-sm text-gray-500 mt-2 text-center site-stats-container"
                 data-analytics-base-url={baseUrl}
                 data-analytics-api-key={apiKey}
                 data-analytics-website-id={websiteId}
                 data-i18n-views={i18n(I18nKey.pageViews)}
                 data-i18n-visitors={i18n(I18nKey.visitors)}
                 data-i18n-error={i18n(I18nKey.statsError)}
            >
                <Icon name="fa6-solid:eye" class="inline-block mr-1 text-gray-400 text-sm align-middle" />
                <span class="site-stats-display">{i18n(I18nKey.statsLoading)}</span>
            </div>
        )}
    </div>
</div>

<style>
    .profile-card {
        position: relative;
        overflow: hidden;
        border: 1px solid color-mix(in srgb, var(--primary) 14%, var(--line-divider));
        background:
            radial-gradient(circle at 50% -15%, color-mix(in oklch, var(--primary) 16%, transparent), transparent 48%),
            var(--card-bg);
        box-shadow: 0 .75rem 2rem color-mix(in srgb, var(--primary) 7%, transparent);
    }
    .profile-card::before { position: absolute; inset: 0 1.25rem auto; height: 2px; content: ""; background: linear-gradient(90deg, transparent, var(--primary), transparent); opacity: .7; }
    .profile-avatar-link { border: 1px solid color-mix(in srgb, var(--primary) 30%, transparent); box-shadow: 0 0 0 .3rem color-mix(in srgb, var(--primary) 7%, transparent), 0 .8rem 1.8rem color-mix(in srgb, var(--primary) 12%, transparent); }
    .profile-avatar-link:hover { border-color: var(--primary); box-shadow: 0 0 0 .3rem color-mix(in srgb, var(--primary) 13%, transparent), 0 .8rem 2rem color-mix(in srgb, var(--primary) 20%, transparent); }
    .profile-social-link { border: 1px solid color-mix(in srgb, var(--primary) 15%, transparent); }
    .profile-social-link:hover { color: var(--primary); border-color: color-mix(in srgb, var(--primary) 44%, transparent); background: color-mix(in oklch, var(--primary) 11%, var(--btn-regular-bg)); transform: translateY(-2px); }
</style>

{analyticsEnabled && (
    <script>
        import { initAnalyticsStats } from "@utils/analytics";
        import "@/plugins/umami-share";

        const scheduleSiteStats = () => requestAnimationFrame(() => window.setTimeout(() => {
            initAnalyticsStats(".site-stats-container", ".site-stats-display", "umami", false);
        }, 0));
        scheduleSiteStats();
        document.addEventListener('astro:page-load', scheduleSiteStats);
    </script>
)}
